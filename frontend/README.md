# Frontend

This is a minimal Vite + React frontend. To run locally:

```bash
cd frontend
npm install
npm run dev
```

To deploy to Vercel: import this repo in Vercel and set the **Root Directory** to `frontend`, or run:

```bash
cd frontend
vercel --prod
```
